name: Orky Automation Bot

on:
  schedule:
    - cron: "01 * * * *"  # Rulează la fiecare 10 minute
  workflow_dispatch:        # Permite pornirea manuală

jobs:
  automation_job:
    name: Run SeleniumBase on Ubuntu
    runs-on: ubuntu-latest
    
    env:
      PY_COLORS: "1"
      PYTHONUNBUFFERED: "1"

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Configure System Locale
        run: |
          sudo apt-get update
          sudo apt-get install -y tzdata locales
          sudo locale-gen en_US.UTF-8
          sudo localectl set-locale LANG="en_US.UTF-8"
          sudo update-locale LANG=en_US.UTF-8

      - name: Install Brave Browser
        run: |
          sudo apt-get install -y curl
          sudo curl -fsSLo /usr/share/keyrings/brave-browser-archive-keyring.gpg https://brave-browser-apt-release.s3.brave.com/brave-browser-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/brave-browser-archive-keyring.gpg] https://brave-browser-apt-release.s3.brave.com/ stable main" | sudo tee /etc/apt/sources.list.d/brave-browser-release.list
          sudo apt-get update
          sudo apt-get install -y brave-browser

      - name: Setup Cloudflare WARP
        run: |
          curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg | sudo gpg --yes --dearmor --output /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
          echo "deb [arch=amd64 signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/cloudflare-client.list
          sudo apt-get update && sudo apt-get install -y --no-install-recommends cloudflare-warp
          sudo warp-cli --accept-tos registration new
          sudo warp-cli --accept-tos mode warp+doh
          sudo warp-cli --accept-tos connect
          sleep 5
          # Verificare conexiune
          curl -s https://www.cloudflare.com/cdn-cgi/trace | grep "warp=on"

      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip wheel
          pip install seleniumbase pyautogui pymongo python-xlib

      - name: Install WebDrivers
        run: |
          # SeleniumBase se va ocupa de gestionarea driverului pentru Brave
          seleniumbase install chromedriver

      - name: Execute Automation Script
        run: |
          python orky.py --brave --incognito --do-not-track --xvfb --screenshot
        continue-on-error: true
